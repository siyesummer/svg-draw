// 节点key字段
export const keyField = "key";

// svg默认配置
export const svgConfig = {
  strokeColor: "#FF4D4F",
  strokeWidth: 4,
  radius: 4,
  //  框选拖拽临界值，超过生效防误触
  areaDragCritical: 5,
  _saved: false,
};

// 圆点批注
export const dotConfirmInputConfig = {
  width: 240,
  height: 120,
  avatarWidth: 40,
  avatarHeight: 40,
};

// 框选批注
export const confirmInputConfig = {
  width: 280,
  height: 100,
  avatarWidth: 44,
  avatarHeight: 44,
  avatarSize: 36,
};

// 文本批注
export const textInputConfig = {
  width: 200,
  height: 80,
};

// svgShape
export const svgShapeConfig = {
  textInput: "text-input",
  confirmInput: "confirm-input",
  areaConfirmInput: "area-confirm-input",
  painting: "painting",
};

// svgSubShape
export const svgSubShapeConfig = {
  rect: "rect",
  confirmInput: "confirm-input",
};

// 批注tooltip
export const annotationsTooltipConfig = {
  width: 270,
  avatarSize: 26,
};
